<div id="loader">
    <div class="progress-bar">
        <div></div>
    </div>
</div>

<style>
    #loader {
    display: none; /* Hidden by default */
    position: fixed;
    top: 0;
    left: 0;
    height: 100vh;
    width: 100vw;
    background-color: white;
    z-index: 999999; /* Ensure it stays on top */
}

.progress-bar {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 300px;
    background-color: rgba(112, 128, 144, 0.466);
    height: 5px;
    border-radius: 5px;
    overflow: hidden; /* Ensure the animated bar doesn't overflow */
}

.progress-bar div {
    height: 100%;
    width: 100%; /* Width of the moving span */
    background-color: black;
    border-radius: 5px;
    animation: move .3s linear infinite; /* Continuous animation */
}

/* Keyframes for the loader movement */
@keyframes move {
    0% {
        transform: translateX(-100%); /* Start off-screen from the left */
    }
    100% {
        transform: translateX(100%); /* Move back to the left side */
    }
}

</style>
